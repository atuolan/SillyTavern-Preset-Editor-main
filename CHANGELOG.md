# Changelog

## 2026-02-27

### v0.2.3 - API 配置页布局美化补丁

- 调整 API 配置行布局比例，扩大配置键名称显示区域。
- 将类型选择与值编辑区域适度右移对齐，减少长键名不必要换行。
- 为类型选择与值编辑输入增加统一宽度约束，改善对齐一致性。
- 版本升级到 `v0.2.3`，并同步文档版本号。
- 发布准备清理：
  - 仓库不再跟踪本地分析文档与参考预设样例文件；
  - README 重写为仓库展示版；
  - CLI 帮助示例改为通用 `./preset.json` 路径。

### v0.2.2 - 配置组功能补丁

- 新增配置组管理能力（提示词顺序配置组）：
  - 新增配置组；
  - 修改配置组ID（`character_id`）；
  - 删除配置组。
- 新增与修改配置组ID时，输入留空将自动分配可用ID。
- 删除配置组增加二次确认弹窗，降低误删风险。
- 主界面新增配置组操作按钮：新增、改ID、删除。
- 版本升级到 `v0.2.2`，并同步项目文档。

### v0.2.1 - 文案减负补丁

- 版本升级到 `v0.2.1`。
- API 类型文案调整为：`文本 / 数值 / 开关 / JSON`。
- 将“布尔值”改为更直观的“开关”。
- 去除冗余注释与过度翻译，重点保留必要术语：
  - `TXT（纯文本，默认）` 简化为 `TXT（纯文本）`；
  - `API 配置（接口参数）` 简化为 `API 配置`；
  - 部分 `（Profile）`、`（key）` 类提示简化。

### v0.2.0 - 美化与汉化小更新

- 新增版本标识：主界面顶部显示 `v0.2.0`。
- 修复正则列表启停状态图标文字溢出问题，改为圆形状态徽标。
- 全面优化界面中文文案：
  - 非专有词改为中文；
  - 专有字段统一采用“中文（英文键名）”标注方式。
- API 配置页增强汉化：
  - 常见配置键提供中文说明；
  - 类型选择显示“文本/数值/布尔值/结构化数据”并附英文键名。
- 文档页重构为分类折叠结构：
  - 分类为“项目使用与介绍、预设变量、正则配置、参考资料”；
  - 默认只展开“项目使用与介绍”。
- 项目文档补充“文案与术语规范”，用于后续迭代约束。

### v0.1.0 - Major update

- 重构 UI 为三标签页：
  - 提示词
  - 正则
  - API配置
- 提示词页支持：
  - 提示词增删改/复制
  - 启用开关与顺序调整
  - 导出格式切换（TXT / JSON）
- 正则页重构：
  - 移除右侧预览
  - 左侧列表 + 中间编辑
  - 导出改为多选控制，且“一个脚本一个 JSON 文件”
  - 未选择正则时导出按钮不可用
- API配置页新增：
  - 预设顶层配置可视化编辑
  - 仅在 JSON 导出勾选时携带
- 导出机制更新：
  - JSON 明文与 JSON 酒馆预设不再区分，统一酒馆风格 JSON
  - JSON 导出支持“全部 / 仅启用”提示词范围，默认“全部”
  - 默认不携带正则与 API 配置
- 工程能力保留并增强：
  - 工程导入/导出持续可用
  - 支持新建空工程，作为从零编写器使用
- 文档能力新增：
  - `web/docs.html` 独立文档页（变量说明、正则说明、操作教程）
  - 新增 `PROJECT.md` 项目总览
  - 新增 `CHANGELOG.md` 更新记录
